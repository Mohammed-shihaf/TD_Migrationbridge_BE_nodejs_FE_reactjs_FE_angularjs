import { IClone, IStatistic, IOptions, ITokenizer, IStore, IMapFrame, Statistic, ISubscriber, IBlamedLines, DetectorEvents, IHandler, ITokenLocation, ICloneValidator, IValidationResult } from '@jscpd/core';
import { Entry } from 'fast-glob';

interface IEntry extends Entry {
    detectedFormat?: string;
}
interface EntryWithContent extends IEntry {
    content: string;
}

interface IHook {
    process(clones: IClone[]): Promise<IClone[]>;
}

interface IReporter {
    report(clones: IClone[], statistic: IStatistic | undefined): void;
}

declare class InFilesDetector {
    private readonly tokenizer;
    private readonly store;
    private readonly statistic;
    readonly options: IOptions;
    private readonly reporters;
    private readonly subscribes;
    private readonly postHooks;
    constructor(tokenizer: ITokenizer, store: IStore<IMapFrame>, statistic: Statistic, options: IOptions);
    registerReporter(reporter: IReporter): void;
    registerSubscriber(subscriber: ISubscriber): void;
    registerHook(hook: IHook): void;
    detect(fls: EntryWithContent[]): Promise<IClone[]>;
}

/**
 * Read gitignore patterns from dirs, walking up to the repo root for each,
 * also reading .git/info/exclude and (optionally) a global excludes file.
 *
 * @param dirs        Directories to scan (typically the jscpd scan paths).
 * @param globalExcludesFile
 *   • undefined (default) — auto-detect via `git config --global core.excludesFile`
 *   • null                — skip global excludes entirely
 *   • string              — use this path directly (for testing / explicit override)
 */
declare function collectGitignorePatterns(dirs: string[], globalExcludesFile?: string | null): string[];
declare function getFilesToDetect(options: IOptions): EntryWithContent[];

declare class BlamerHook implements IHook {
    process(clones: IClone[]): Promise<IClone[]>;
    static blameLines(clone: IClone): Promise<IClone>;
    static getBlamedLines(blamedFiles: Record<string, IBlamedLines>, start: number, end: number): IBlamedLines;
}

declare class FragmentsHook implements IHook {
    process(clones: IClone[]): Promise<IClone[]>;
    static addFragments(clone: IClone): IClone;
}

declare class ProgressSubscriber implements ISubscriber {
    private readonly options;
    constructor(options: IOptions);
    subscribe(): Partial<Record<DetectorEvents, IHandler>>;
}

declare class VerboseSubscriber implements ISubscriber {
    protected options: IOptions;
    constructor(options: IOptions);
    subscribe(): Partial<Record<DetectorEvents, IHandler>>;
}

declare class ConsoleReporter implements IReporter {
    private readonly options;
    constructor(options: IOptions);
    report(clones: IClone[], statistic?: IStatistic | undefined): void;
}

declare class ConsoleFullReporter implements IReporter {
    private readonly options;
    constructor(options: IOptions);
    report(clones: IClone[]): void;
    private cloneFullFound;
}

interface IDuplication {
    format: string;
    lines: number;
    tokens: number;
    firstFile: {
        name: string;
        start: number;
        end: number;
        startLoc: ITokenLocation;
        endLoc: ITokenLocation;
        blame?: IBlamedLines;
    };
    secondFile: {
        name: string;
        start: number;
        end: number;
        startLoc: ITokenLocation;
        endLoc: ITokenLocation;
        blame?: IBlamedLines;
    };
    fragment: string;
}
interface IJsonReport {
    duplicates: IDuplication[];
    statistics: IStatistic;
}
declare class JsonReporter implements IReporter {
    private options;
    constructor(options: IOptions);
    generateJson(clones: IClone[], statistics: IStatistic): IJsonReport;
    report(clones: IClone[], statistic: IStatistic): void;
    private cloneFound;
}

declare class CSVReporter implements IReporter {
    private options;
    constructor(options: IOptions);
    report(clones: IClone[], statistic: IStatistic | undefined): void;
}

declare class MarkdownReporter implements IReporter {
    private options;
    constructor(options: IOptions);
    report(clones: IClone[], statistic: IStatistic | undefined): void;
}

declare class XmlReporter implements IReporter {
    private options;
    constructor(options: IOptions);
    report(clones: IClone[]): void;
}

declare class SilentReporter implements IReporter {
    report(clones: IClone[], statistic: IStatistic): void;
}

declare class ThresholdReporter implements IReporter {
    private options;
    constructor(options: IOptions);
    report(clones: IClone[], statistic: IStatistic | undefined): void;
}

declare class XcodeReporter implements IReporter {
    private readonly options;
    constructor(options: IOptions);
    report(clones: IClone[]): void;
    private cloneFound;
}

declare function compressCloneLine(pathA: string, pathB: string, rangeA: string, rangeB: string): string;
declare class AiReporter implements IReporter {
    private readonly options;
    constructor(options: IOptions);
    report(clones: IClone[], statistic: IStatistic | undefined): void;
}

declare class SkipLocalValidator implements ICloneValidator {
    validate(clone: IClone, options: IOptions): IValidationResult;
    shouldSkipClone(clone: IClone, options: IOptions): boolean;
    private static isRelative;
}

declare function parseFormatsExtensions(extensions?: string): {
    [key: string]: string[];
} | undefined;
declare const parseFormatsNames: typeof parseFormatsExtensions;

interface ColorResolutionInput {
    /**
     * Explicit override from CLI/config (`--colors` / `--no-colors` /
     * `colors: true|false`). `undefined` means "auto-detect".
     */
    colors?: boolean;
    env?: NodeJS.ProcessEnv;
    isTTY?: boolean;
}
/**
 * Decide whether ANSI colors should be emitted, following the precedence:
 *
 *   1. explicit CLI/config override (`--colors` / `--no-colors`);
 *   2. `FORCE_COLOR` (force on, unless `0`/`false`);
 *   3. `NO_COLOR` (force off);
 *   4. TTY detection of the output stream;
 *   5. default (no color when the stream is not a TTY).
 *
 * See https://no-color.org and the widely used `FORCE_COLOR` convention.
 * `FORCE_COLOR` is checked first so that opting back in works inside images
 * that export `NO_COLOR` globally, matching Node core (`tty.getColorDepth`),
 * chalk, and `@colors/colors`.
 */
declare function shouldEnableColors(input: ColorResolutionInput): boolean;
/**
 * Apply the resolved color preference to the shared `colors` singleton so that
 * every reporter using `colors/safe` respects it. Reads `process.stdout.isTTY`
 * and the environment when no explicit override is provided.
 */
declare function configureColors(options?: {
    colors?: boolean;
}, env?: NodeJS.ProcessEnv, isTTY?: boolean): boolean;

/**
 * Attach the standard verbose/progress subscribers to a detector, skipping
 * the progress announcer when a configured reporter prints every clone
 * itself. Shared by the jscpd CLI and jscpd-server so the double-print
 * exclusion cannot drift between them.
 */
declare function registerSubscribers(options: IOptions, detector: InFilesDetector): void;

export { AiReporter, BlamerHook, CSVReporter, type ColorResolutionInput, ConsoleFullReporter, ConsoleReporter, type EntryWithContent, FragmentsHook, type IEntry, type IHook, type IReporter, InFilesDetector, JsonReporter, MarkdownReporter, ProgressSubscriber, SilentReporter, SkipLocalValidator, ThresholdReporter, VerboseSubscriber, XcodeReporter, XmlReporter, collectGitignorePatterns, compressCloneLine, configureColors, getFilesToDetect, parseFormatsExtensions, parseFormatsNames, registerSubscribers, shouldEnableColors };
